public interface PaymentMethod {

    // Abstract methods (wajib diimplementasikan class yang implements)
    String processPayment();      // memproses pembayaran
    String getPaymentDetails();   // informasi metode pembayaran
    double getTransactionFee();   // biaya transaksi
    double getBalance();          // mengembalikan saldo
}
